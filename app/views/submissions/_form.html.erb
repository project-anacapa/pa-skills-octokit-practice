<%= form_for([@course,@project,@submission]) do |f| %>
  <% if @submission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@submission.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @submission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :github_owner %><br>
    <%= f.text_field :github_owner %>
  </div>
  <div class="field">
    <%= f.label :github_repo %><br>
    <%= f.text_field :github_repo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
